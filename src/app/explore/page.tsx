import { Metadata } from "next";
import styles from "./explore.module.scss";

export const metadata: Metadata = {
  title: "Explore – ImageHub",
  description: "Search for your favorite artwork.",

  
};

export default function Page() {
  return (
    <main>
      <></>
    </main>
  );
}